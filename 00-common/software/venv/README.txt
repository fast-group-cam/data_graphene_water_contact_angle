Install a Python virtual environment in this directory, and install the `contact-angle` package (included in this repository as a submodule; alternatively, install the v0.3.0 branch manually from https://github.com/fast-group-cam/contact-angle).

Example:

>    python -m venv ./
>    source ./bin/activate
>    pip install ../contact-angle

